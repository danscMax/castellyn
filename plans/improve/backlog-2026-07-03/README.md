# Бэклог улучшений 2026-07-03 — исполнение субагентами без контекста

Источник: `plans/improve/report-2026-07-03.md` (доказательная база), `fix_plan-2026-07-03.json`
(машиночитаемый статус approved/needs_confirmation). Каждая задача ниже самодостаточна:
свежий агент выполняет её, прочитав ТОЛЬКО этот файл волны + указанные файлы кода.

## Порядок волн и зависимости

| Волна | Файл | Тем | Зависимости |
|---|---|---|---|
| 1 | wave-1-hardening.md | security + подтверждённые баги (6 задач) | нет |
| 2 | wave-2-quick-wins.md | дешёвые улучшения (8 задач) | нет (2.2 до 4.4/4.5) |
| 3 | wave-3-reliability.md | надёжность (6 задач) | нет |
| 4 | wave-4-ux.md | UX-переделки (7 задач) | 4.4/4.5 после 2.2 |
| 5 | wave-5-limits.md | лимиты Anthropic + автопродолжение (5 задач) | 5.2→5.1, 5.3→5.1+5.2, 5.4→5.3, 5.5→5.1 |

Внутри волны задачи независимы (если не указано иное) — можно параллелить по одному агенту
на задачу, НО: задачи, правящие один файл (пометка `⚠ same-file`), выполнять последовательно.

## Обязательные правила для каждого агента

1. **Прочитай `CLAUDE.md` репозитория** (E:\Scripts\Castellyn\CLAUDE.md) — конвенции обязательны.
   Ключевое: DRY (переиспользуй `spawn_streamed`, `askConfirm`, `common.*`); каждый user-facing
   текст через `t('ns.key')` с паритетом **ru/en/zh** (`npm run check:i18n`); НИКОГДА не называй
   переменную цикла/параметр `t`; все спавны процессов с `CREATE_NO_WINDOW` (0x08000000);
   JSON от PowerShell — strip BOM; никакой AI-атрибуции в коммитах.
2. **Гейты после задачи** (все зелёные до коммита):
   `npm run check` (0 errors/0 warnings), `npm test`, `npm run check:i18n`,
   `cargo test` в src-tauri (cargo НЕ в PATH — полный путь `%USERPROFILE%\.cargo\bin\cargo.exe`;
   под PowerShell не верь коду возврата, проверяй `$LASTEXITCODE`), `npm run build`.
   После целой волны дополнительно: `.\build_all.ps1` (релизный exe).
3. **Git**: ветка `improve-0703/wave-N` от main; коммит на задачу
   (`fix(scope): …` / `feat(scope): …`); перед КАЖДЫМ коммитом `git status` — чужие файлы
   (например `.serena/project.yml`) НЕ коммитить. Merge в main — ff-only, после гейтов волны.
4. **Строки кода в задачах** даны на состояние коммита 4b9d3e5 — если файл сдвинулся, ищи по
   приведённому фрагменту, а не по номеру строки.
5. **Не расширяй скоуп**: делай ровно задачу; замеченные чужие баги — в отчёт, не в код.
6. **UI-задачи**: прогони ?shot-смоук (dev-харнес `npm run dev` + `/?shot`, mock-IPC из
   `src/lib/shot/fixtures.ts`), проверь ОБЕ темы, 0 console errors.
7. Отчёт агента: что сделано, какие гейты прогнаны (с числами), что НЕ сделано и почему.

## Статус

Утверждено владельцем: волна 5 целиком (решения 2026-07-03 в задачах). Волны 1-4 — план по
рекомендованным дефолтам; развилки помечены `needs_confirmation` в задачах: реши по
рекомендации и отметь решение в отчёте, НЕ блокируйся.
