{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "panes",
  "description": "Detached per-monitor / popped-out pane windows. They render ONLY a terminal mirror (DetachedView), so they get window controls + clipboard-write + ipc — but NOT the native file dialogs or the OS opener that the main window has.",
  "windows": ["mon-*", "pane-*"],
  "permissions": [
    "core:default",
    "core:window:allow-minimize",
    "core:window:allow-unminimize",
    "core:window:allow-maximize",
    "core:window:allow-unmaximize",
    "core:window:allow-is-maximized",
    "core:window:allow-start-dragging",
    "core:window:allow-close",
    "clipboard-manager:allow-write-text"
  ]
}
