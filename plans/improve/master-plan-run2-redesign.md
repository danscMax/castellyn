# Мастер-план: Прогон 2 + herdr-редизайн Сессий (оптимальный порядок)

Дата: 2026-07-03. Цель — сделать **и** Прогон 2 (11 пунктов), **и** herdr-редизайн Сессий так, чтобы
**Sessions-верстку не переделывать дважды**. Заменяет монолитный «Прогон 2 = 11 пунктов» из
`RUN2-prompt.md`.

## Принцип разделения (почему такой порядок)
Из 11 пунктов Прогона 2 к **верстке Сессий** привязаны только **3** (13, 10, 21d-чип). Остальные
**8** — логика/данные/не-Sessions, они **переживут** редизайн. Значит:
- redesign-safe пункты → **делаем сейчас** (Фаза A), редизайн их не тронет;
- layout-coupled пункты → **вплавляем в редизайн** (Фаза B), делаем один раз;
- `18` (durable sidecar) — **первым**: на это хранилище встанет и новая персонализация редизайна.

---

## ГЕЙТ (сначала, на тебе)
Ре-смоук 6 smoke-фиксов (`f96fb5d`) → зелено → **FF-merge Run 1 в main**. Всё ниже — после этого.

---

## ФАЗА A — Прогон 2, redesign-safe (делаем сейчас, свежая `/max:dev`)
Внутренний порядок: сначала фундамент, потом остальное.

| id | Пункт | Где | Почему безопасно к редизайну |
|----|-------|-----|------------------------------|
| **18** | Дыра 1: durable sidecar `~/.claude/castellyn/sessions.json` + миграция + Syncthing + бэкап | data | **фундамент** — на нём и будущая персонализация редизайна; делать первым |
| #5 | гигиена хуков (маркер-скан осиротевших профилей) | backend | нет UI |
| 21c | авто-продолжение после сброса лимита | логика | не зависит от верстки |
| 19 | Shift+PgUp/PgDn scrollback + Alt+N фокус панели | TerminalPane | панель переживает редизайн |
| 17 | создать один профиль без reinstall всех | ProfilesTab | не Сессии |
| 15 | «Обновить всё» + авто-recheck + прогресс | +page (Обновления) | не Сессии |
| 11 | SSH-светофор emoji → `.dot` | Сессии (мелочь) | маленький элемент, переживёт |
| 21e | режим после лимита: ожидание vs переключение на свободный профиль | backend/логика | не зависит от верстки |
| 21d* | колонка utilization в **Профилях** (только эта часть) | ProfilesTab | не Сессии |

_(NB: пункт 16 «atLimit disable ▶» — уже сделан в Прогоне 1, Волна 2; в Прогон 2 не входит.)_

→ После Фазы A: можно резать **v0.7.0** (25 фиксов Run 1 + Фаза A) ЛИБО ждать Фазу B для одного
релиза A+B — решим по факту (быстрый релиз безопаснее, см. вариант «Быстрый v0.7.0»).

---

## ФАЗА B — herdr-редизайн Сессий (свой дизайн-заход)
**Начинается с ДИЗАЙН-фазы, не сразу код:** brainstorm → 2-3 макета (ASCII/HTML) → твой выбор → сборка.
herdr под AGPL — код НЕ копируем, повторяем UX своим.

Что входит:
- **Новая верстка Сессий:** spaces-вкладки проектов (сверху) + список **agents** (сбоку/снизу) +
  **компактная шапка** — освободить место под терминал (главная боль: сейчас шапка ест много).
- **Индикатор завершения на самой панели** (визуально в окне, а не только Windows-тост).
- **ВПЛАВЛЯЮТСЯ сюда** (чтобы не делать дважды):
  - `13` keep-mounted вкладок → часть новой модели вкладок/spaces;
  - `10` единый цвет sidebar↔tab (blocked/done) → в новой верстке;
  - `21d-чип` «5h NN% · сброс» → в новой компактной шапке.

→ После Фазы B: **v0.8.0** (или общий v0.7.0 = A+B, если решим копить).

---

## Вне плана (по желанию)
- **Дыры 2/4** (MCP-реконсиляция «хвостов»; Syncthing-адрес из config.xml) — мелкий хардненинг,
  не про Сессии. Можно вклинить бонусом в Фазу A или отдельным заходом.
- **PLAUSIBLE-баги** ревью (Home broken-filter guard; set_shortcuts pre-register) — тоже в Фазу A бонусом.
- **Анонс OSS** (0 звёзд → главный рычаг) — маркетинг, на владельце, в любой момент.

## Итог по «не переделывать дважды»
- 8 пунктов → сейчас, редизайн их не тронет.
- 3 пункта (13/10/21d-чип) → внутри редизайна, один раз.
- 18 (sidecar) первым → durable-дом и для новой персонализации.
- Sessions-верстка меняется РОВНО ОДИН раз — в Фазе B.
