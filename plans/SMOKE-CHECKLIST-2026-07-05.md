# Живой смоук v0.7.0 — чек-лист владельца (2026-07-05)

Накопленный слой: control-plane Ф1–Ф5 + матрица V1/V2 + лаунчер C + GC. Всё зелёное по
гейтам и ?shot; ниже — только то, что нельзя проверить без живого Tauri/реальных данных.
Запуск: свежесобранный `castellyn.exe` (build_all.ps1) или ярлык Castellyn.lnk.

Порядок подобран от безопасного к пишущему. Каждый пункт = 1-3 минуты.

## 1. Главная — карта дрейфа (read-only)
- [ ] Карточка «Дрейф стека» показывает 4 пункта (plugin_sync файл/разводка, managed, marketplace_versions).
- [ ] Пункт marketplace показывает реальное отставание (max 1.9.0 < 1.14.1, speckit 1.0.1 < 1.0.2) — подписи не перепутаны.

## 2. Главная — GC-карточка (пишет: Корзина, обратимо)
- [ ] Карточка «Мусор» показывает итог (ожидаемо: stale max 1.7.0 / ponytail 4.7.0 / superpowers 5.1.0×2 / ui-ux-pro-max 2.5.0 + wrong_os ~125 МБ report-only).
- [ ] «Показать и почистить…» → модал; wrong_os без чекбокса.
- [ ] Удалить выбранное → confirm → тост с освобождёнными МБ → элементы В КОРЗИНЕ (проверь Корзину).
- [ ] Активные версии (claude-mem 13.8.1, security-guidance 2.0.6) в списке НЕ появлялись.

## 3. Плагины — bump и «Обновить всё» (пишет: манифесты + claude plugin update)
- [ ] На «своей» строке (max) дропдоун ⇪ → patch: стрим лога, оба манифеста бампнуты, установленная версия подтянулась.
- [ ] «Обновить всё» подтягивает max/speckit до source-версий; после — drift-пункт marketplace на Главной гаснет.

## 4. Профили — матрица (пишет: settings.json / profiles.json / .claude.json профилей)
- [ ] Смена провайдера/прокси на тестовом профиле → предпросмотр → Применить → перечитка показывает новое значение.
- [ ] Папки: снять/добавить shared-папку → apply → relink; реальные данные не тронуты (только junction'ы).
- [ ] ПЛАГИНЫ: выключить один плагин у одного профиля → apply → в settings.json профиля появился явный `false`; после следующего plugin_sync ключ НЕ вернулся в true.
- [ ] MCP-чип: у профиля с недостающим сервером «додеплоить» → чип стал K/K; у профиля с extra — ✕ → confirm → extra снят.
- [ ] Попытка правки под ЖИВОЙ сессией профиля → отказ с понятным сообщением (guard).

## 5. Сессии — лаунчер C
- [ ] В проекте с живой панелью «＋» (главная зона) мгновенно запускает агента по рецепту панели (без формы).
- [ ] В пустом проекте «＋» открывает форму.
- [ ] Шеврон: меню с «Недавние» (после пары запусков — до 5, без дублей), «Избранные ★», «Свой запуск…».
- [ ] Запуск из недавних воспроизводит профиль/папку/аргументы.

## 6. Ре-смоук ложного «готово» (f96fb5d)
- [ ] Длинная задача агенту Claude: пауза вывода 5-30 сек НЕ даёт тост «Агент закончил»; реальный Stop — даёт.

## 7. Сироты профилей (v0.7.0, пишет: Корзина)
- [ ] Создать `~/.claude-zzztest` с пустым `.claude.json` → панель «Посторонние каталоги» видит → Delete → в Корзине.

---
Результаты (ок/фейл + заметки) — сюда или в чат; по зелёному итогу ставим тег v0.7.0.
