{
  "wave": 1,
  "zones": {
    "rust": { "files": ["src-tauri/src/lib.rs", "src-tauri/src/i18n.rs"], "items": ["1a", "1b", "1e", "1f"] },
    "frontend": { "files": ["src/lib/components/SessionsTab.svelte", "src/lib/components/TerminalPane.svelte"], "items": ["1c", "1d"] }
  },
  "conflicts": "none (zones file-disjoint)",
  "readOnly": [
    "existing #[cfg(test)] tests in src-tauri/src/lib.rs (may ADD new tests, never modify/delete existing)",
    "src/lib/i18n/** (no new frontend strings needed in wave 1)",
    ".serena/project.yml", "plans/**", "manifest/**", "tools/**"
  ],
  "i18nKeys": {
    "err.mcp_unsafe_chars": "NEW backend key in src-tauri/src/i18n.rs, 3 locales [ru,en,zh], placeholder {name}; pattern follows err.mcp_* block at i18n.rs:82-87"
  },
  "sharedTypes": {
    "onIdChange": "(key: string, id: string | null) => void — existing prop TerminalPane -> SessionsTab, signature FROZEN"
  },
  "commandSignatures": {
    "probe_provider": "fn probe_provider(base_url:&str, protocol:&str, api_key:&str) -> serde_json::Value — signature FROZEN, returns {ok,detail} json on reject",
    "add_provider_key": "fn add_provider_key(id:String, api_key:String) -> Result<MyProvider,String> — signature FROZEN",
    "codex_mcp_add_args": "fn codex_mcp_add_args(name:&str, def:&Value) -> Option<Vec<String>> — signature FROZEN (guard lives in a separate testable helper)"
  },
  "cmdMetacharSet": "& | < > ^ % \" (decision_resolved 1a; applies to every argv element that reaches `cmd /C codex`)",
  "loopbackAllowlist": "localhost, 127.0.0.1 (any 127.* ), [::1] — http:// allowed only for these hosts in probe_provider (decision_resolved 1b)"
}
