.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Runtime output of the vendored fork-updater (written next to its script)
tools/fork-updater/fork-sync.last.json
tools/fork-updater/logs/

# Playwright MCP scratch output
.playwright-mcp/

# Windows Explorer folder-view turds
desktop.ini

# Audit/design/report working artifacts: written here by tooling and agents, kept on disk, never
# committed. Plans already in the index stay tracked (gitignore does not affect tracked files); a
# genuinely durable new plan needs an explicit `git add -f`.
plans/
providers-snap.md
canon-audit-report.md
